# Server-only settings. Configure via Sites; never commit real values.
CONTACT_FORM_ENABLED=false
RESEND_API_KEY=
CONTACT_FROM_EMAIL=

# Alternative: existing Google Workspace send-only relay
GOOGLE_WORKSPACE_RELAY_URL=
GOOGLE_WORKSPACE_RELAY_SECRET=
